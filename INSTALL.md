# Markdown Reader 安装与试用

## 安装

### 1. 找到安装包
位置：`02-Gadgets/md-reader/dist/`
- **`Markdown Reader_1.0.0_x64-setup.exe`** ← **推荐**（NSIS，5 语言安装界面，体验更友好）
- `Markdown Reader_1.0.0_x64_en-US.msi` — MSI 备选（适合企业部署）

### 2. 双击 .exe 安装包

会出现 **Microsoft Defender SmartScreen 警告**（"未知发布者"）—— 这是正常的，因为没买 ¥1500/年的代码签名证书。

**操作**：点 **更多信息** → **仍要运行**。

### 3. 安装界面（5 语言）

- 选择安装语言（English / 简体中文 / 繁體中文 / 日本語 / 한국어）
- 选择安装位置（默认 `%LOCALAPPDATA%\Programs\Markdown Reader\`，**仅当前用户**，无需管理员权限）
- **勾选 "Set as default for .md files"**（默认勾选）
- 点 Install

### 4. 安装完成

桌面 / 开始菜单 出现 "Markdown Reader" 图标。

---

## 验收清单（请逐项打勾）

> 测试时建议用项目里 `tests/sample.md` 这个文件，里面涵盖了所有渲染元素。

### 🟢 基础打开
- [ ] 双击 `tests/sample.md` 能用 Markdown Reader 打开（前提：勾选了"设为默认"）
- [ ] 桌面 / 开始菜单图标点击能启动应用（无文件，显示欢迎屏）
- [ ] 拖拽 `.md` 文件到窗口能加载

### 🟢 渲染（用 sample.md 测）
- [ ] H1 / H2 / H3 / H4 标题大小正确，字重一致 Notion 风
- [ ] 有序 / 无序列表样式正常（圆点 + 缩进）
- [ ] 任务列表（`- [x]`）显示复选框
- [ ] 引用块有左侧竖线
- [ ] 表格边框 / 表头清晰
- [ ] 代码块**Python / JavaScript** 有语法高亮颜色
- [ ] 行内代码 `code` 有红色 + 灰底
- [ ] 链接是蓝色，hover 出下划线
- [ ] **加粗 / 斜体 / 删除线** 都对

### 🟢 模式切换
- [ ] 默认进入是**阅读模式**（看到渲染漂亮的版本）
- [ ] **Ctrl+E** 切换到编辑模式（看到原始文本）
- [ ] 编辑模式下右上按钮变 **✏️ 编辑**
- [ ] 改文字后，状态栏出现 **● 未保存** 标识
- [ ] **Ctrl+S** 保存，标识消失
- [ ] 切回阅读模式（再按 Ctrl+E）能看到刚才的改动渲染好

### 🟢 快捷键（编辑模式下测）
- [ ] **Ctrl+C / V / X / A** 标准复制粘贴剪切全选
- [ ] **Ctrl+Z / Y** 撤销重做
- [ ] **Ctrl+F** 弹查找浮窗，输入关键字高亮匹配，**Enter** 跳下一个
- [ ] **Ctrl+O** 打开文件对话框
- [ ] **Ctrl++ / -- / 0** 字号放大、缩小、重置
- [ ] **F1** 弹出使用说明
- [ ] **F11** 全屏 / 退出全屏

### 🟢 五语言切换
点右上角 🌐 图标：
- [ ] 简体中文（默认）→ 菜单是"文件 / 编辑 / 视图 / 帮助"
- [ ] 繁體中文 → "檔案 / 編輯 / 檢視 / 說明"
- [ ] English → "File / Edit / View / Help"
- [ ] 日本語 → "ファイル / 編集 / 表示 / ヘルプ"
- [ ] 한국어 → "파일 / 편집 / 보기 / 도움말"
- [ ] 切语言**不需要重启**应用

### 🟢 主题切换
点右上角 🌗 图标，三态循环：
- [ ] 浅色（白底）
- [ ] 深色（深灰底，文字浅）
- [ ] 跟随系统（看你 Win11 的浅 / 深设置）
- [ ] 深色模式下**代码块也是深色**（不是亮色突兀）

### 🟢 文件关联
- [ ] 装时勾选了"设默认" → Windows 资源管理器双击 `.md` 自动用 Markdown Reader 打开
- [ ] 没勾选 → 用：菜单 文件 → 设为 .md 默认打开方式
- [ ] 还可以右键 `.md` 文件 → 打开方式 → Markdown Reader

### 🟢 大文件保护
- [ ] **5MB+** 的 `.md` 文件打开时弹"文件较大"确认对话框
- [ ] **20MB+** 文件被拒绝（弹错误对话框，建议用专业编辑器）

### 🟢 编码兼容
- [ ] **UTF-8** 编码 `.md` 正常打开（中文不乱码）
- [ ] **GBK** 编码 `.md`（老 Windows 默认）也能打开（自动识别）

### 🟢 大纲 (TOC)
- [ ] 左侧大纲列出所有 H1-H4
- [ ] 点击大纲项**滚动**到对应标题
- [ ] **Ctrl+\\** 显示 / 隐藏大纲

### 🟢 可选插件
菜单 帮助 → 启用 Mermaid / KaTeX：
- [ ] 启用 **Mermaid 流程图** → `.md` 里 \`\`\`mermaid 块渲染成流程图
- [ ] 启用 **LaTeX 数学公式** → `$E=mc^2$` 渲染成漂亮公式
- [ ] 关闭后回到代码块状态

### 🟢 卸载
- [ ] **设置 → 应用 → 已安装的应用** → 找到 "Markdown Reader" → 卸载
- [ ] 卸载后桌面 / 开始菜单图标消失
- [ ] 卸载后 `.md` 文件关联**不**残留（再双击 `.md` 不会用本应用打开）

### 🟢 性能 / 体感
- [ ] 启动 < 3 秒
- [ ] 打开 1MB `.md` 几乎无延迟
- [ ] 切语言 / 切主题瞬时响应
- [ ] 滚动顺滑、不卡顿、不闪烁

---

## 测试中遇到问题

### A. SmartScreen 警告吓人
正常现象（未签名应用都会有）。如果你打算分享给朋友，告诉他们点"仍要运行"。

### B. 双击 .md 没反应 / 用别的程序打开
- 检查安装时是否勾选了"设默认"
- 没勾选：右键 `.md` 文件 → 打开方式 → 选择其他应用 → Markdown Reader → 勾选"始终用此应用打开"

### C. 中文乱码
默认按 UTF-8 解码。如果 `.md` 是 GBK 编码（老 Windows 记事本默认），应用会**自动识别**。如果不行，反馈给我。

### D. 启动慢 / 闪退
- 第一次启动可能要等 1-2 秒（Windows 加载 WebView2）
- 如果闪退：尝试在控制面板**修复 Microsoft Edge WebView2 Runtime**

### E. 中文标题大纲跳转不准
偶发问题。反馈给我，我修。

---

## 报告问题

测试中发现的任何问题，反馈这几样：
1. **截图**（按 Win+Shift+S 截屏）
2. **复现步骤**（"打开 X 文件 → 按 Y → 看到 Z"）
3. **预期 vs 实际**

修复后我重新打安装包，你再测一遍。

---

## 通过测试后

- 你说"OK"，我把这个工程推到 GitHub Releases（按你说的"测试通过才上 GitHub"）
- 后续可以让朋友直接从 GitHub 下载安装包
